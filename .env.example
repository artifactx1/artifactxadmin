# ElementServer base URL — used by next.config.js rewrites to proxy /api/*.
# In dev: http://localhost:5001 (matches PORT=5001 in ElementServer/.env).
# In prod: the deployed API domain (set as BACKEND_URL on Railway).
BACKEND_URL=http://localhost:5001
