# ElementServer base URL — used by next.config.js rewrites to proxy /api/*.
# In dev: http://localhost:3000 (the API server).
# In prod: the deployed API domain.
ELEMENT_SERVER_URL=http://localhost:3000
